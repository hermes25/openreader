import React, {
    requireNativeComponent
} from 'react-native';

/**
 * 文本框模块
 * @module TextStorage
 */
var RCTRTextView = requireNativeComponent('RCTRTextView', null);

module.exports = RCTRTextView;
